# 前端美化整合实施方案

## 项目背景

### 一、现状分析

本项目存在两个前端版本：

| 版本 | 路径 | 特点 |
|------|------|------|
| **origin（美化原版）** | `D:\Project\Agent\origin` | 精心设计的视觉系统，230行CSS设计系统，SVG流程图，毛玻璃导航栏，Linear/Vercel风格 |
| **agent-system（功能版）** | `D:\Project\Agent\agent-system\frontend` | 完整后端对接，WebSocket实时通信，认证系统，10个页面，但视觉简化 |

### 二、核心差异对比

#### 2.1 视觉设计层面

| 维度 | origin | agent-system |
|------|--------|--------------|
| CSS设计系统 | ✅ 完整（230行变量+组件类） | ❌ 缺失（仅3行Tailwind指令） |
| 色彩体系 | ✅ 14个CSS变量（中性色阶+强调色+语义色） | ❌ 无自定义变量 |
| 导航栏 | ✅ 毛玻璃效果（backdrop-filter: blur） | ⚠️ 普通Tailwind样式 |
| 卡片组件 | ✅ 自定义.card类，精致边框圆角 | ⚠️ 依赖Nuxt UI默认样式 |
| 按钮系统 | ✅ 5种变体（primary/ghost/text/lg/sm） | ⚠️ 依赖UButton组件 |
| 进度条 | ✅ 自定义.bar类，语义色支持 | ⚠️ 依赖UProgress组件 |
| 状态指示 | ✅ 脉冲动画（.pulse）+ 状态点 | ⚠️ 静态UBadge |
| 排版系统 | ✅ .prose富文本样式 | ❌ 缺失 |

#### 2.2 功能层面

| 维度 | origin | agent-system |
|------|--------|--------------|
| 后端API对接 | ❌ 全部硬编码mock数据 | ✅ 完整REST API封装（useApi.ts） |
| WebSocket | ❌ 无 | ✅ 实时Agent状态（useAgentWebSocket.ts） |
| 认证系统 | ❌ 无登录页，硬编码"陈同学" | ✅ 完整login/register/JWT |
| 页面数量 | 7个 | 10个（+login/register/history） |
| 数据驱动 | ❌ 静态展示 | ✅ 动态API数据 |

#### 2.3 组件层面

| 组件 | origin | agent-system | 整合策略 |
|------|--------|--------------|----------|
| Agent流程图 | SVG手绘流程图（精美但静态） | 列表卡片+进度条（功能性强） | ⭐ **动态化改造**：保留origin的精美SVG视觉，添加实时状态和动画 |
| 知识雷达图 | 命令式ECharts（精美配置） | 声明式vue-echarts（普通配置） | ⭐ **配置迁移**：保留动态数据绑定，应用origin的精美ECharts配置 |
| 难度匹配曲线 | 命令式ECharts（精美配置） | 声明式vue-echarts（普通配置） | ⭐ **配置迁移**：保留动态数据绑定，应用origin的精美ECharts配置 |
| 资源Tab | 无 | ResourceTabs.vue | 保留agent-system版本 |

#### 2.4 特殊页面

| 页面 | 说明 | 整合策略 |
|------|------|----------|
| 登录页 | origin无此页面，agent-system使用Nuxt UI组件 | ⭐ **完全重写**：使用origin设计风格，禁用导航栏布局 |
| 注册页 | origin无此页面，agent-system使用Nuxt UI组件 | ⭐ **完全重写**：使用origin设计风格，禁用导航栏布局 |

---

## 实施可行性评估

### 三、可行性分析

#### 3.1 技术栈兼容性 ✅ 完全兼容

两个项目使用**完全相同**的技术栈：
- 框架：Nuxt 3 ^3.15.0
- 视图层：Vue 3 ^3.5.0
- CSS方案：Tailwind CSS + @nuxt/ui
- 图表库：ECharts + vue-echarts
- 语言：TypeScript

**结论**：CSS设计系统和页面模板可以直接迁移，无需修改技术栈。

#### 3.2 代码结构兼容性 ✅ 高度兼容

两个项目的目录结构一致：
- `assets/css/` - 样式文件
- `components/` - 组件
- `composables/` - 组合式函数
- `layouts/` - 布局
- `pages/` - 页面
- `types/` - 类型定义

**结论**：文件路径对应关系明确，迁移成本低。

#### 3.3 功能保留可行性 ✅ 完全可行

agent-system的后端对接能力通过以下文件实现：
- `composables/useApi.ts` - API封装
- `composables/useAgentWebSocket.ts` - WebSocket
- `composables/useAuth.ts` - 认证
- `composables/useSession.ts` - 会话

这些文件与视觉设计**完全解耦**，可以在迁移视觉设计时完整保留。

#### 3.4 风险评估

| 风险 | 等级 | 应对措施 |
|------|------|----------|
| CSS变量与Tailwind冲突 | 低 | CSS变量定义在:root，Tailwind可直接引用 |
| Nuxt UI组件样式覆盖 | 中 | 使用CSS特异性或!important覆盖默认样式 |
| 响应式断点不一致 | 低 | origin使用760px/900px，Tailwind使用md/lg，可统一 |
| 页面布局差异 | 中 | 逐页面对比调整，保留agent-system的动态数据绑定 |

---

## 实施步骤

### 四、详细实施计划

#### 第一阶段：CSS设计系统迁移（预计2-3小时）

##### 步骤1.1：复制origin的CSS设计系统

**操作**：将`origin/assets/css/main.css`的完整内容复制到`agent-system/frontend/assets/css/main.css`

**文件**：`agent-system/frontend/assets/css/main.css`

**内容**：
```css
@tailwind base;
@tailwind components;
@tailwind utilities;

/* ============================================================
   领域知识个性化生成与多智能体协同决策系统 · 设计系统
   ============================================================ */

:root {
  /* 中性色阶 */
  --bg:          #FFFFFF;
  --bg-soft:     #FAFAFA;
  --bg-muted:    #F5F5F5;
  --text:        #111827;
  --text-2:      #4B5563;
  --text-3:      #9CA3AF;
  --line:        #ECECEF;
  --line-2:      #E5E7EB;
  /* 强调色 */
  --accent:      #4F46E5;
  --accent-soft: #EEF2FF;
  /* 语义色 */
  --ok:          #059669;
  --ok-soft:     #ECFDF5;
  --warn:        #D97706;
  --warn-soft:   #FFFBEB;
  --err:         #DC2626;
  --err-soft:    #FEF2F2;
  /* 字体 */
  --font: -apple-system, BlinkMacSystemFont, "Segoe UI", "PingFang SC",
          "Hiragino Sans GB", "Microsoft YaHei", "Helvetica Neue", sans-serif;
  --mono: "SF Mono", "JetBrains Mono", "Fira Code", Consolas, monospace;
  --radius: 10px;
  --radius-sm: 6px;
  --nav-h: 56px;
  --maxw: 1080px;
}

/* 全局 reset */
*, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }
html { -webkit-font-smoothing: antialiased; -moz-osx-font-smoothing: grayscale; }
body {
  font-family: var(--font);
  background: var(--bg);
  color: var(--text);
  font-size: 14px;
  line-height: 1.6;
  letter-spacing: -.01em;
}
a { color: inherit; text-decoration: none; }
button { font-family: inherit; cursor: pointer; border: none; background: none; }
ul { list-style: none; }
svg { display: block; }

/* 顶部导航、页面布局、卡片、按钮、Badge、状态点、进度条、表单、Prose、工具类、脉冲动画... */
/* （完整内容见 origin/assets/css/main.css） */
```

**验证**：启动前端开发服务器，确认无CSS编译错误。

##### 步骤1.2：在Tailwind配置中注册CSS变量

**操作**：创建或修改`tailwind.config.js`，将CSS变量映射为Tailwind颜色

**文件**：`agent-system/frontend/tailwind.config.js`（新建）

**内容**：
```javascript
/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    './components/**/*.{vue,js,ts}',
    './layouts/**/*.vue',
    './pages/**/*.vue',
    './composables/**/*.{js,ts}',
    './plugins/**/*.{js,ts}',
    './app.vue',
  ],
  theme: {
    extend: {
      colors: {
        bg: {
          DEFAULT: 'var(--bg)',
          soft: 'var(--bg-soft)',
          muted: 'var(--bg-muted)',
        },
        text: {
          DEFAULT: 'var(--text)',
          2: 'var(--text-2)',
          3: 'var(--text-3)',
        },
        line: {
          DEFAULT: 'var(--line)',
          2: 'var(--line-2)',
        },
        accent: {
          DEFAULT: 'var(--accent)',
          soft: 'var(--accent-soft)',
        },
        ok: {
          DEFAULT: 'var(--ok)',
          soft: 'var(--ok-soft)',
        },
        warn: {
          DEFAULT: 'var(--warn)',
          soft: 'var(--warn-soft)',
        },
        err: {
          DEFAULT: 'var(--err)',
          soft: 'var(--err-soft)',
        },
      },
      fontFamily: {
        sans: ['var(--font)'],
        mono: ['var(--mono)'],
      },
      borderRadius: {
        DEFAULT: 'var(--radius)',
        sm: 'var(--radius-sm)',
      },
      height: {
        nav: 'var(--nav-h)',
      },
      maxWidth: {
        content: 'var(--maxw)',
      },
    },
  },
  plugins: [],
}
```

**验证**：在任意Vue文件中使用`bg-accent`、`text-text-2`等类名，确认颜色正确应用。

---

#### 第二阶段：布局系统迁移（预计1-2小时）

##### 步骤2.1：迁移默认布局

**操作**：将origin的`layouts/default.vue`替换agent-system的版本，同时保留认证功能

**文件**：`agent-system/frontend/layouts/default.vue`

**整合策略**：
1. 保留origin的毛玻璃导航栏样式（.topnav类）
2. 保留agent-system的登录/注册/退出功能
3. 保留agent-system的移动端汉堡菜单
4. 添加origin的底部版权信息

**关键代码结构**：
```vue
<template>
  <div class="min-h-screen flex flex-col">
    <!-- 毛玻璃导航栏 -->
    <header class="topnav">
      <NuxtLink to="/" class="topnav__brand">
        <span class="dot"></span>
        领域知识个性化生成系统
      </NuxtLink>

      <nav class="topnav__items">
        <NuxtLink to="/profile" class="topnav__link">学习者画像</NuxtLink>
        <NuxtLink to="/dashboard" class="topnav__link">学情诊断</NuxtLink>
        <!-- ... 其他导航项 ... -->
      </nav>

      <div class="topnav__right">
        <!-- 认证状态：登录/注册 或 用户头像+退出 -->
        <template v-if="isAuthenticated">
          <div class="topnav__user">
            <span class="topnav__avatar">{{ userInitial }}</span>
            <span>{{ userName }}</span>
          </div>
          <button class="btn btn--ghost btn--sm" @click="logout">退出</button>
        </template>
        <template v-else>
          <NuxtLink to="/login" class="btn btn--ghost btn--sm">登录</NuxtLink>
          <NuxtLink to="/register" class="btn btn--primary btn--sm">注册</NuxtLink>
        </template>
      </div>
    </header>

    <!-- 主内容区 -->
    <main class="flex-1">
      <slot />
    </main>

    <!-- 底部版权 -->
    <footer class="py-6 text-center text-text-3 text-xs border-t border-line">
      © 2026 领域知识个性化生成与多智能体协同决策系统
    </footer>
  </div>
</template>
```

**验证**：
- 导航栏显示毛玻璃效果
- 登录状态正确显示用户信息
- 移动端菜单正常工作
- 当前页面高亮正确

---

#### 第三阶段：页面迁移（预计6-8小时）

##### 步骤3.1：首页迁移（index.vue）

**优先级**：⭐⭐⭐ 最高（用户第一印象）

**操作**：将origin的首页布局迁移到agent-system，保留动态数据能力

**整合策略**：
1. 保留origin的Hero区域设计（标题+描述+CTA按钮）
2. 保留origin的6个Agent介绍卡片网格
3. 保留origin的4步学习闭环展示
4. 添加origin的AgentFlowDiagram SVG组件
5. 保留agent-system的动态数据绑定（如有）

**文件变更**：
- `agent-system/frontend/pages/index.vue` - 替换为origin版本
- `agent-system/frontend/components/AgentFlowDiagram.vue` - 从origin复制

**关键代码**：
```vue
<template>
  <div>
    <!-- Hero区域 -->
    <section class="page" style="padding-top: 80px; padding-bottom: 60px;">
      <div class="page-head" style="text-align: center; max-width: 640px; margin: 0 auto;">
        <h1 class="page-head__title" style="font-size: 36px;">
          领域知识个性化生成与多智能体协同决策系统
        </h1>
        <p class="page-head__desc" style="margin: 16px auto 0;">
          基于学习者画像，通过多个AI Agent协同工作，智能生成个性化学习资源
        </p>
        <div style="margin-top: 32px; display: flex; gap: 12px; justify-content: center;">
          <NuxtLink to="/profile" class="btn btn--primary btn--lg">开始学习</NuxtLink>
          <NuxtLink to="/dashboard" class="btn btn--ghost btn--lg">查看演示</NuxtLink>
        </div>
      </div>
    </section>

    <!-- Agent介绍 -->
    <section class="page" style="padding-top: 0;">
      <h2 class="page-head__title" style="font-size: 22px; text-align: center; margin-bottom: 32px;">
        六大智能体协同工作
      </h2>
      <div class="agent-grid">
        <!-- 6个Agent卡片 -->
      </div>
    </section>

    <!-- 学习闭环 -->
    <section class="page page--wide" style="padding-top: 0;">
      <h2 class="page-head__title" style="font-size: 22px; text-align: center; margin-bottom: 32px;">
        四步学习闭环
      </h2>
      <AgentFlowDiagram />
    </section>
  </div>
</template>
```

**验证**：
- Hero区域显示正确，按钮可点击
- Agent卡片网格布局正常
- SVG流程图渲染正确

---

##### 步骤3.2：学习者画像页迁移（profile.vue）

**优先级**：⭐⭐⭐ 高（核心功能入口）

**操作**：整合origin的表单样式和agent-system的API对接

**整合策略**：
1. 保留origin的表单样式（.field、.input、.select类）
2. 保留agent-system的技能列表（CNC数控加工领域）
3. 保留agent-system的API调用逻辑
4. 添加origin的toast通知样式

**文件变更**：`agent-system/frontend/pages/profile.vue`

**关键改动**：
```vue
<template>
  <div class="page">
    <div class="page-head">
      <p class="page-head__eyebrow">Step 1</p>
      <h1 class="page-head__title">学习者画像</h1>
      <p class="page-head__desc">请填写您的学习背景和目标，以便系统为您生成个性化学习资源</p>
    </div>

    <div class="card">
      <div class="card__head">
        <h2 class="card__title">基本信息</h2>
      </div>

      <div class="field">
        <label class="field__label">学历层次</label>
        <select v-model="form.education" class="select">
          <option value="">请选择</option>
          <option value="high_school">高中</option>
          <option value="college">大专</option>
          <option value="bachelor">本科</option>
          <option value="master">硕士及以上</option>
        </select>
      </div>

      <!-- 其他表单字段... -->

      <button class="btn btn--primary btn--lg" @click="submitProfile">
        提交画像
      </button>
    </div>
  </div>
</template>
```

**验证**：
- 表单样式与origin一致
- 提交成功后显示toast通知
- API调用正常工作

---

##### 步骤3.3：学情诊断仪表盘迁移（dashboard.vue）

**优先级**：⭐⭐⭐ 高（核心数据展示）

**操作**：整合origin的卡片样式和agent-system的数据绑定

**整合策略**：
1. 保留origin的4个统计指标卡片设计
2. 保留origin的自定义进度条样式（.bar类）
3. 保留agent-system的KnowledgeRadar组件（ECharts）
4. 保留agent-system的API数据获取逻辑
5. 添加origin的诊断建议提示框

**文件变更**：`agent-system/frontend/pages/dashboard.vue`

**关键改动**：
```vue
<template>
  <div class="page page--wide">
    <div class="page-head">
      <p class="page-head__eyebrow">Step 2</p>
      <h1 class="page-head__title">学情诊断</h1>
      <p class="page-head__desc">基于您提交的学习者画像，系统将为您生成个性化学习路径</p>
    </div>

    <!-- 统计指标卡片 -->
    <div class="stats-grid">
      <div class="card">
        <div class="card__head">
          <span class="card__title">整体掌握度</span>
          <span class="badge badge--accent">{{ overallMastery }}%</span>
        </div>
        <div class="bar">
          <div class="bar__fill" :style="{ width: overallMastery + '%' }"></div>
        </div>
      </div>
      <!-- 其他指标卡片... -->
    </div>

    <!-- 知识雷达图 -->
    <div class="card" style="margin-top: 24px;">
      <div class="card__head">
        <h2 class="card__title">知识掌握雷达</h2>
      </div>
      <KnowledgeRadar :data="radarData" />
    </div>

    <!-- 知识盲区定位 -->
    <div class="card" style="margin-top: 24px;">
      <div class="card__head">
        <h2 class="card__title">知识盲区定位</h2>
        <span class="badge badge--err">{{ blindSpots.length }} 个盲区</span>
      </div>
      <div class="blind-spot-list">
        <div v-for="spot in blindSpots" :key="spot.name" class="blind-spot-item">
          <span class="dot dot--err"></span>
          <span>{{ spot.name }}</span>
          <span class="muted">{{ spot.description }}</span>
        </div>
      </div>
    </div>
  </div>
</template>
```

**验证**：
- 统计卡片样式与origin一致
- 雷达图正常渲染
- 数据从API动态获取

---

##### 步骤3.4：Agent协同页迁移（workflow.vue）

**优先级**：⭐⭐ 中

**操作**：保留agent-system的WebSocket功能，应用origin的样式

**整合策略**：
1. 保留agent-system的WebSocket实时连接
2. 应用origin的Agent状态卡片样式
3. 保留agent-system的触发生成按钮
4. 添加origin的连接状态指示器样式

**文件变更**：`agent-system/frontend/pages/workflow.vue`

**关键改动**：
```vue
<template>
  <div class="page page--wide">
    <div class="page-head">
      <p class="page-head__eyebrow">Step 3</p>
      <h1 class="page-head__title">Agent 协同</h1>
      <p class="page-head__desc">实时查看7个AI Agent的协同工作状态</p>
    </div>

    <!-- 连接状态 -->
    <div class="card" style="margin-bottom: 24px;">
      <div class="card__head">
        <span class="card__title">实时连接</span>
        <span :class="['badge', isConnected ? 'badge--ok' : 'badge--err']">
          {{ isConnected ? '已连接' : '未连接' }}
        </span>
      </div>
      <div v-if="isConnected" class="pulse" style="display: inline-block;">
        <span class="dot dot--ok"></span>
      </div>
    </div>

    <!-- Agent状态列表 -->
    <div class="agent-status-grid">
      <div v-for="agent in agents" :key="agent.id" class="card">
        <div class="card__head">
          <span class="card__title">{{ agent.name }}</span>
          <span :class="getStatusBadgeClass(agent.status)">
            {{ getStatusText(agent.status) }}
          </span>
        </div>
        <div class="bar">
          <div class="bar__fill" :style="{ width: agent.progress + '%' }"></div>
        </div>
        <p class="muted" style="margin-top: 8px; font-size: 12px;">
          {{ agent.description }}
        </p>
      </div>
    </div>

    <!-- 触发生成按钮 -->
    <div style="margin-top: 32px; text-align: center;">
      <button class="btn btn--primary btn--lg" @click="triggerGeneration">
        触发资源生成
      </button>
    </div>
  </div>
</template>
```

**验证**：
- WebSocket连接状态正确显示
- Agent状态实时更新
- 脉冲动画正常工作

---

##### 步骤3.5：资源展示页迁移（resources.vue）

**优先级**：⭐⭐ 中

**操作**：整合origin的Tab样式和agent-system的Markdown渲染

**整合策略**：
1. 保留origin的Tab切换样式
2. 保留agent-system的Markdown渲染逻辑
3. 保留agent-system的知识来源标注高亮
4. 添加origin的source-tag样式

**文件变更**：`agent-system/frontend/pages/resources.vue`

---

##### 步骤3.6：反馈交互页迁移（practice.vue）

**优先级**：⭐⭐ 中

**操作**：整合origin的答题界面样式和agent-system的苏格拉底追问

**整合策略**：
1. 保留origin的答题卡片样式
2. 保留agent-system的多轮苏格拉底追问（最多3轮）
3. 保留agent-system的API调用逻辑
4. 添加origin的动态调整记录展示

**文件变更**：`agent-system/frontend/pages/practice.vue`

---

##### 步骤3.7：分析报告页迁移（report.vue）

**优先级**：⭐⭐ 中

**操作**：整合origin的核心指标样式和agent-system的数据绑定

**整合策略**：
1. 保留origin的核心指标卡片设计（谬误率/匹配率/覆盖率）
2. 保留origin的进度条样式
3. 保留agent-system的DifficultyMatchCurve组件
4. 保留agent-system的学习路径数据

**文件变更**：`agent-system/frontend/pages/report.vue`

---

##### 步骤3.8：登录/注册页重写（login.vue / register.vue）

**优先级**：⭐⭐⭐ 高（用户入口）

**操作**：完全重写登录和注册页面，使用origin的设计风格，禁用导航栏

**设计要求**：
1. **禁用导航栏**：使用`definePageMeta({ layout: false })`禁用默认布局
2. **居中卡片布局**：垂直水平居中，最大宽度420px
3. **背景装饰**：添加渐变圆形装饰元素，增加视觉层次
4. **品牌标识**：顶部显示系统名称和靛蓝色圆点
5. **表单样式**：使用origin的CSS变量（--accent, --text, --line等）
6. **图标**：使用内联SVG图标（用户名、邮箱、密码）
7. **密码强度指示器**：注册页面添加密码强度条
8. **动画效果**：输入框聚焦动画、按钮hover效果、背景浮动动画

**文件变更**：
- `agent-system/frontend/pages/login.vue` - 完全重写
- `agent-system/frontend/pages/register.vue` - 完全重写

**关键代码结构**：
```vue
<template>
  <div class="auth-page">
    <!-- 背景装饰 -->
    <div class="auth-bg">
      <div class="auth-bg__circle auth-bg__circle--1"></div>
      <div class="auth-bg__circle auth-bg__circle--2"></div>
      <div class="auth-bg__circle auth-bg__circle--3"></div>
    </div>

    <!-- 登录卡片 -->
    <div class="auth-container">
      <!-- 品牌标识 -->
      <div class="auth-brand">
        <span class="auth-brand__dot"></span>
        <span class="auth-brand__text">领域知识个性化生成系统</span>
      </div>

      <!-- 表单卡片 -->
      <div class="auth-card">
        <div class="auth-card__header">
          <h1 class="auth-card__title">欢迎回来</h1>
          <p class="auth-card__subtitle">登录您的账号以继续学习</p>
        </div>

        <form class="auth-form" @submit.prevent="handleLogin">
          <!-- 用户名输入框 -->
          <div class="auth-field">
            <label class="auth-field__label">用户名</label>
            <div class="auth-input-wrapper">
              <svg class="auth-input__icon"><!-- 用户图标 --></svg>
              <input v-model="formState.username" class="auth-input" placeholder="请输入用户名" />
            </div>
          </div>

          <!-- 密码输入框 -->
          <div class="auth-field">
            <label class="auth-field__label">密码</label>
            <div class="auth-input-wrapper">
              <svg class="auth-input__icon"><!-- 锁图标 --></svg>
              <input v-model="formState.password" type="password" class="auth-input" placeholder="请输入密码" />
            </div>
          </div>

          <!-- 登录按钮 -->
          <button type="submit" class="auth-btn auth-btn--primary" :disabled="loading">
            <span>登录</span>
          </button>
        </form>

        <div class="auth-card__footer">
          <p>还没有账号？<NuxtLink to="/register" class="auth-link">立即注册</NuxtLink></p>
        </div>
      </div>
    </div>
  </div>
</template>

<script setup lang="ts">
// ✅ 关键：禁用默认布局（不显示导航栏）
definePageMeta({
  layout: false,
})

// ... 业务逻辑保持不变
</script>

<style scoped>
/* 使用origin的CSS变量 */
.auth-page {
  min-height: 100vh;
  display: flex;
  align-items: center;
  justify-content: center;
  background: var(--bg);
}

.auth-card {
  background: var(--bg);
  border: 1px solid var(--line);
  border-radius: var(--radius);
  padding: 40px 32px;
}

.auth-input:focus {
  border-color: var(--accent);
  box-shadow: 0 0 0 3px var(--accent-soft);
}

.auth-btn--primary {
  background: var(--accent);
  color: #fff;
}
</style>
```

**验证**：
- 页面顶部无导航栏
- 表单样式与origin风格一致
- 登录/注册功能正常
- 响应式布局正常

---

##### 步骤3.9：历史记录页保留（history.vue）

**优先级**：⭐ 低

**操作**：保留agent-system的history.vue，应用统一的样式系统

**文件变更**：`agent-system/frontend/pages/history.vue`（样式调整）

---

#### 第四阶段：核心组件动态化改造（预计5-7小时）

> **核心目标**：将origin的精美视觉效果与agent-system的实时功能相结合，实现"既好看又能动"的组件。

##### 步骤4.1：知识雷达图动态化（预计1小时）

**现状对比**：

| 维度 | origin | agent-system | 目标 |
|------|--------|--------------|------|
| 视觉效果 | ⭐⭐⭐⭐⭐ 精美 | ⭐⭐⭐ 普通 | ⭐⭐⭐⭐⭐ 精美 |
| 数据绑定 | ❌ 静态硬编码 | ✅ 动态props | ✅ 动态props |
| 动画交互 | ✅ ECharts内置 | ✅ ECharts内置 | ✅ ECharts内置 |

**整合策略**：保留agent-system的声明式API（vue-echarts）和动态数据绑定，应用origin的精美ECharts配置。

**文件**：`agent-system/frontend/components/dashboard/KnowledgeRadar.vue`

**完整实现代码**：

```vue
<template>
  <div class="w-full" style="max-width: 380px; height: 360px; margin: 0 auto;">
    <VChart v-if="isValidData" :option="chartOption" autoresize />
    <p v-else class="text-gray-400 text-center py-12">暂无有效数据</p>
  </div>
</template>

<script setup lang="ts">
import VChart from 'vue-echarts'
import { use } from 'echarts/core'
import { RadarChart } from 'echarts/charts'
import { CanvasRenderer } from 'echarts/renderers'

use([RadarChart, CanvasRenderer])

interface KnowledgePoint {
  name: string
  score: number
  level: string
}

const props = defineProps<{
  knowledgePoints: KnowledgePoint[]
}>()

const isValidData = computed(() => {
  return Array.isArray(props.knowledgePoints) &&
    props.knowledgePoints.length > 0 &&
    props.knowledgePoints.every((kp) => kp && kp.name && typeof kp.score === 'number')
})

// ✅ 使用origin的精美配置 + agent-system的动态数据
const chartOption = computed(() => ({
  radar: {
    indicator: props.knowledgePoints.map((kp) => ({
      name: kp.name,
      max: 100,
    })),
    shape: 'polygon',
    splitNumber: 4,  // origin: 4层分割，更简洁
    axisName: {
      color: '#4B5563',
      fontSize: 11,
      fontFamily: '-apple-system, BlinkMacSystemFont, "PingFang SC", "Microsoft YaHei", sans-serif',
    },
    splitArea: { show: false },  // origin: 隐藏分割区域背景
    splitLine: { lineStyle: { color: '#ECECEF' } },  // origin: 浅灰色网格线
    axisLine: { lineStyle: { color: '#ECECEF' } },   // origin: 浅灰色轴线
  },
  series: [{
    type: 'radar',
    data: [{
      value: props.knowledgePoints.map((kp) => kp.score),  // ✅ 动态数据
      areaStyle: { color: 'rgba(79, 70, 229, 0.08)' },    // origin: 淡靛蓝半透明
      lineStyle: { color: '#4F46E5', width: 1.5 },         // origin: 靛蓝色线条
      itemStyle: { color: '#4F46E5', borderWidth: 0 },     // origin: 靛蓝色数据点
      symbol: 'circle',
      symbolSize: 6,                                        // origin: 显示数据点
    }],
  }],
}))
</script>
```

**效果**：
- ✅ 精美视觉：靛蓝色配色、浅灰网格线、无背景区域
- ✅ 动态数据：从API获取知识点评分
- ✅ 动画交互：ECharts内置的hover提示、缩放等

---

##### 步骤4.2：难度匹配曲线动态化（预计1小时）

**现状对比**：

| 维度 | origin | agent-system | 目标 |
|------|--------|--------------|------|
| 图表类型 | 纯折线图（精美） | 柱状图+折线图（普通） | ⭐ 纯折线图（精美） |
| 渐变背景 | ✅ 有 | ❌ 无 | ✅ 有 |
| 学习者水平线 | 灰色虚线 | 红色虚线 | ✅ 灰色虚线 |
| 数据绑定 | ❌ 静态 | ✅ 动态 | ✅ 动态 |

**文件**：`agent-system/frontend/components/report/DifficultyMatchCurve.vue`

**完整实现代码**：

```vue
<template>
  <div class="w-full" style="max-width: 480px; height: 280px; margin: 0 auto;">
    <VChart v-if="isValidData" :option="chartOption" autoresize />
    <p v-else class="text-gray-400 text-center py-12">暂无有效数据</p>
  </div>
</template>

<script setup lang="ts">
import VChart from 'vue-echarts'
import { use } from 'echarts/core'
import { LineChart } from 'echarts/charts'
import { GridComponent, MarkLineComponent } from 'echarts/components'
import { CanvasRenderer } from 'echarts/renderers'
import { graphic } from 'echarts'

use([LineChart, GridComponent, MarkLineComponent, CanvasRenderer])

interface MatchData {
  learnerLevel: number
  resources: {
    name: string
    difficulty: number
    matchScore: number
  }[]
}

const props = defineProps<{
  data: MatchData
}>()

const isValidData = computed(() => {
  return props.data && 
    Array.isArray(props.data.resources) && 
    props.data.resources.length > 0
})

// ✅ 使用origin的精美配置 + agent-system的动态数据
const chartOption = computed(() => ({
  grid: { top: 30, right: 40, bottom: 40, left: 50 },
  xAxis: {
    type: 'category',
    data: props.data.resources.map((r) => r.name),  // ✅ 动态数据
    axisLine: { lineStyle: { color: '#E5E7EB' } },
    axisTick: { show: false },
    axisLabel: { color: '#9CA3AF', fontSize: 10 },
  },
  yAxis: {
    type: 'value',
    min: 1,
    max: 5,
    interval: 1,
    axisLabel: {
      formatter: (v: number) => `L${v}`,  // origin: L1/L2/L3格式
      color: '#9CA3AF',
      fontSize: 10,
      fontFamily: 'SF Mono, JetBrains Mono, Consolas, monospace',
    },
    splitLine: { lineStyle: { color: '#F3F4F6' } },
    axisLine: { show: false },
    axisTick: { show: false },
  },
  series: [
    {
      type: 'line',  // origin: 纯折线图（非柱状图）
      data: props.data.resources.map((r) => r.difficulty),  // ✅ 动态数据
      smooth: false,
      symbol: 'circle',
      symbolSize: 7,
      lineStyle: { color: '#4F46E5', width: 2 },           // origin: 靛蓝色线条
      itemStyle: { color: '#4F46E5', borderWidth: 0 },     // origin: 靛蓝色数据点
      areaStyle: {                                          // origin: 渐变背景
        color: new graphic.LinearGradient(0, 0, 0, 1, [
          { offset: 0, color: 'rgba(79, 70, 229, 0.12)' },
          { offset: 1, color: 'rgba(79, 70, 229, 0.02)' },
        ]),
      },
      markLine: {
        silent: true,
        symbol: 'none',
        lineStyle: { color: '#9CA3AF', width: 1.4, type: 'dashed' },  // origin: 灰色虚线
        label: {
          show: true,
          position: 'insideEndTop',
          formatter: '学习者水平',
          color: '#9CA3AF',
          fontSize: 10,
        },
        data: [{ yAxis: props.data.learnerLevel }],  // ✅ 动态数据
      },
    },
  ],
}))
</script>
```

**效果**：
- ✅ 精美视觉：纯折线图、靛蓝渐变背景、灰色虚线参考线
- ✅ 动态数据：从API获取资源难度和学习者水平
- ✅ 动画交互：ECharts内置的hover提示

---

##### 步骤4.3：Agent流程图动态化（预计3-4小时）⭐ 核心难点

**现状对比**：

| 维度 | origin | agent-system | 目标 |
|------|--------|--------------|------|
| 视觉效果 | ⭐⭐⭐⭐⭐ 精美SVG | ⭐⭐⭐ 列表卡片 | ⭐⭐⭐⭐⭐ 精美SVG |
| 实时状态 | ❌ 静态 | ✅ WebSocket | ✅ WebSocket |
| 动画效果 | ❌ 无 | ⚠️ 进度条 | ✅ 节点脉冲+连线流动 |
| 交互能力 | ❌ 无 | ✅ 触发按钮 | ✅ 触发按钮 |

**整合策略**：将origin的静态SVG改造为动态SVG，绑定WebSocket实时数据，添加CSS动画效果。

**文件**：`agent-system/frontend/components/agent-view/AgentFlowDiagram.vue`（新建）

**架构设计**：

```
┌─────────────────────────────────────────────────────────────┐
│                    AgentFlowDiagram.vue                       │
├─────────────────────────────────────────────────────────────┤
│  ┌─────────────┐    ┌─────────────┐    ┌─────────────┐     │
│  │  SVG模板    │    │  状态计算   │    │  CSS动画    │     │
│  │  (origin)   │◄───│  (computed) │◄───│  (keyframes)│     │
│  └─────────────┘    └─────────────┘    └─────────────┘     │
│         ▲                  ▲                                  │
│         │                  │                                  │
│  ┌─────────────┐    ┌─────────────┐                         │
│  │  Props绑定  │    │  WebSocket  │                         │
│  │  (动态数据) │◄───│  (实时状态) │                         │
│  └─────────────┘    └─────────────┘                         │
└─────────────────────────────────────────────────────────────┘
```

**完整实现代码**：

```vue
<template>
  <svg 
    viewBox="0 0 900 510" 
    xmlns="http://www.w3.org/2000/svg" 
    class="agent-flow-diagram"
  >
    <defs>
      <!-- 箭头标记（默认灰色） -->
      <marker id="arr" viewBox="0 0 10 10" refX="9" refY="5" markerWidth="6" markerHeight="6" orient="auto">
        <path d="M0,0 L10,5 L0,10" fill="none" stroke="#9CA3AF" stroke-width="1.4"/>
      </marker>
      <!-- 箭头标记（激活状态靛蓝色） -->
      <marker id="arrActive" viewBox="0 0 10 10" refX="9" refY="5" markerWidth="6" markerHeight="6" orient="auto">
        <path d="M0,0 L10,5 L0,10" fill="none" stroke="#4F46E5" stroke-width="1.4"/>
      </marker>
      <!-- 箭头标记（完成状态绿色） -->
      <marker id="arrCompleted" viewBox="0 0 10 10" refX="9" refY="5" markerWidth="6" markerHeight="6" orient="auto">
        <path d="M0,0 L10,5 L0,10" fill="none" stroke="#059669" stroke-width="1.4"/>
      </marker>
    </defs>

    <!-- ========== 决策调度 Agent ========== -->
    <g :class="['agent-node', getAgentStatusClass('dispatcher')]">
      <rect 
        x="240" y="12" width="200" height="46" rx="10" 
        :fill="getAgentFill('dispatcher')" 
        :stroke="getAgentStroke('dispatcher')" 
        stroke-width="1.5"
      />
      <circle cx="262" cy="35" r="4" :fill="getAgentDotColor('dispatcher')"/>
      <text x="274" y="32" font-family="sans-serif" font-size="13" font-weight="600" fill="#111827">
        决策调度 Agent
      </text>
      <text x="274" y="48" font-family="sans-serif" font-size="10.5" fill="#9CA3AF">
        {{ getAgentDescription('dispatcher') }}
      </text>
      <!-- 进度指示器（仅运行时显示） -->
      <rect 
        v-if="getAgentStatus('dispatcher') === 'running'"
        x="240" y="54" width="200" height="3" rx="1.5"
        fill="#E5E7EB"
      />
      <rect 
        v-if="getAgentStatus('dispatcher') === 'running'"
        x="240" y="54" :width="getAgentProgress('dispatcher') * 2" height="3" rx="1.5"
        fill="#4F46E5"
        class="progress-bar"
      />
    </g>

    <!-- 连接线：决策调度 → 学情分析 -->
    <line 
      x1="340" y1="58" x2="340" y2="84" 
      :stroke="getConnectionColor('dispatcher', 'analyzer')"
      stroke-width="1.4" 
      :marker-end="getConnectionMarker('dispatcher', 'analyzer')"
      :class="{ 'flowing-line': isConnectionActive('dispatcher', 'analyzer') }"
    />

    <!-- ========== 学情分析 Agent ========== -->
    <g :class="['agent-node', getAgentStatusClass('analyzer')]">
      <rect 
        x="240" y="86" width="200" height="46" rx="10" 
        :fill="getAgentFill('analyzer')" 
        :stroke="getAgentStroke('analyzer')" 
        stroke-width="1"
      />
      <circle cx="262" cy="109" r="4" :fill="getAgentDotColor('analyzer')"/>
      <text x="274" y="106" font-family="sans-serif" font-size="13" font-weight="600" fill="#111827">
        学情分析 Agent
      </text>
      <text x="274" y="122" font-family="sans-serif" font-size="10.5" fill="#9CA3AF">
        {{ getAgentDescription('analyzer') }}
      </text>
    </g>

    <!-- 连接线：学情分析 → 路径规划 -->
    <line 
      x1="340" y1="132" x2="340" y2="158" 
      :stroke="getConnectionColor('analyzer', 'planner')"
      stroke-width="1.4" 
      :marker-end="getConnectionMarker('analyzer', 'planner')"
      :class="{ 'flowing-line': isConnectionActive('analyzer', 'planner') }"
    />

    <!-- ========== 路径规划 Agent ========== -->
    <g :class="['agent-node', getAgentStatusClass('planner')]">
      <rect 
        x="240" y="160" width="200" height="46" rx="10" 
        :fill="getAgentFill('planner')" 
        :stroke="getAgentStroke('planner')" 
        stroke-width="1"
      />
      <circle cx="262" cy="183" r="4" :fill="getAgentDotColor('planner')"/>
      <text x="274" y="180" font-family="sans-serif" font-size="13" font-weight="600" fill="#111827">
        路径规划 Agent
      </text>
      <text x="274" y="196" font-family="sans-serif" font-size="10.5" fill="#9CA3AF">
        {{ getAgentDescription('planner') }}
      </text>
    </g>

    <!-- 入口折线：路径规划 → 知识生成 -->
    <path 
      d="M 340 206 L 340 272 L 190 272 L 190 290" 
      fill="none" 
      :stroke="getConnectionColor('planner', 'generator')"
      stroke-width="1.4" 
      :marker-end="getConnectionMarker('planner', 'generator')"
      :class="{ 'flowing-line': isConnectionActive('planner', 'generator') }"
    />

    <!-- ========== 辩论闭环虚线框 ========== -->
    <rect 
      x="50" y="232" width="810" height="160" rx="12" 
      fill="none" 
      stroke="#4F46E5" 
      stroke-width="1.2" 
      stroke-dasharray="5 4" 
      opacity=".55"
    />
    <text x="70" y="254" font-family="sans-serif" font-size="11" font-weight="600" fill="#4F46E5" letter-spacing=".04em">
      辩论与独立裁判机制 · 降低知识谬误率
    </text>

    <!-- ========== 知识生成 Agent ========== -->
    <g :class="['agent-node', getAgentStatusClass('generator')]">
      <rect 
        x="95" y="290" width="190" height="52" rx="10" 
        :fill="getAgentFill('generator')" 
        :stroke="getAgentStroke('generator')" 
        stroke-width="1"
      />
      <circle cx="117" cy="316" r="4" :fill="getAgentDotColor('generator')"/>
      <text x="129" y="312" font-family="sans-serif" font-size="13" font-weight="600" fill="#111827">
        知识生成 Agent
      </text>
      <text x="129" y="330" font-family="sans-serif" font-size="10.5" fill="#9CA3AF">
        {{ getAgentDescription('generator') }}
      </text>
    </g>

    <!-- 连接线：知识生成 → 审核纠偏 -->
    <line 
      x1="285" y1="316" x2="349" y2="316" 
      :stroke="getConnectionColor('generator', 'reviewer')"
      stroke-width="1.4" 
      :marker-end="getConnectionMarker('generator', 'reviewer')"
      :class="{ 'flowing-line': isConnectionActive('generator', 'reviewer') }"
    />
    <text x="317" y="308" text-anchor="middle" font-family="sans-serif" font-size="10" fill="#4F46E5">提交</text>

    <!-- ========== 审核纠偏 Agent ========== -->
    <g :class="['agent-node', getAgentStatusClass('reviewer')]">
      <rect 
        x="355" y="290" width="190" height="52" rx="10" 
        :fill="getAgentFill('reviewer')" 
        :stroke="getAgentStroke('reviewer')" 
        stroke-width="1"
      />
      <circle cx="377" cy="316" r="4" :fill="getAgentDotColor('reviewer')"/>
      <text x="389" y="312" font-family="sans-serif" font-size="13" font-weight="600" fill="#111827">
        审核纠偏 Agent
      </text>
      <text x="389" y="330" font-family="sans-serif" font-size="10.5" fill="#9CA3AF">
        {{ getAgentDescription('reviewer') }}
      </text>
    </g>

    <!-- 连接线：审核纠偏 → 裁判 -->
    <line 
      x1="545" y1="316" x2="609" y2="316" 
      :stroke="getConnectionColor('reviewer', 'judge')"
      stroke-width="1.4" 
      :marker-end="getConnectionMarker('reviewer', 'judge')"
      :class="{ 'flowing-line': isConnectionActive('reviewer', 'judge') }"
    />
    <text x="577" y="308" text-anchor="middle" font-family="sans-serif" font-size="10" fill="#4F46E5">分歧</text>

    <!-- ========== 裁判 Agent ========== -->
    <g :class="['agent-node', getAgentStatusClass('judge')]">
      <rect 
        x="615" y="290" width="190" height="52" rx="10" 
        :fill="getAgentFill('judge')" 
        :stroke="getAgentStroke('judge')" 
        stroke-width="1"
      />
      <circle cx="637" cy="316" r="4" :fill="getAgentDotColor('judge')"/>
      <text x="649" y="312" font-family="sans-serif" font-size="13" font-weight="600" fill="#111827">
        裁判 Agent
      </text>
      <text x="649" y="330" font-family="sans-serif" font-size="10.5" fill="#9CA3AF">
        {{ getAgentDescription('judge') }}
      </text>
    </g>

    <!-- 回环连线：裁判 → 知识生成（需修正） -->
    <path 
      d="M 660 342 C 660 374, 190 374, 190 344" 
      fill="none" 
      :stroke="getLoopConnectionColor('judge', 'generator')"
      stroke-width="1.4" 
      stroke-dasharray="4 3" 
      :marker-end="getLoopConnectionMarker('judge', 'generator')"
      :class="{ 'flowing-line': isLoopConnectionActive('judge', 'generator') }"
    />
    <text x="425" y="370" text-anchor="middle" font-family="sans-serif" font-size="10.5" fill="#4F46E5">
      需修正 → 重新生成
    </text>

    <!-- 连接线：裁判 → 试题生成（裁决通过） -->
    <line 
      x1="760" y1="342" x2="760" y2="422" 
      :stroke="getConnectionColor('judge', 'quiz')"
      stroke-width="1.4" 
      :marker-end="getConnectionMarker('judge', 'quiz')"
      :class="{ 'flowing-line': isConnectionActive('judge', 'quiz') }"
    />
    <text x="768" y="386" font-family="sans-serif" font-size="10.5" fill="#4F46E5">裁决通过</text>

    <!-- ========== 试题生成 Agent ========== -->
    <g :class="['agent-node', getAgentStatusClass('quiz')]">
      <rect 
        x="665" y="424" width="190" height="46" rx="10" 
        :fill="getAgentFill('quiz')" 
        :stroke="getAgentStroke('quiz')" 
        stroke-width="1.5"
      />
      <circle cx="687" cy="447" r="4" :fill="getAgentDotColor('quiz')"/>
      <text x="699" y="444" font-family="sans-serif" font-size="13" font-weight="600" fill="#111827">
        试题生成 Agent
      </text>
      <text x="699" y="460" font-family="sans-serif" font-size="10.5" fill="#9CA3AF">
        {{ getAgentDescription('quiz') }}
      </text>
    </g>
  </svg>
</template>

<script setup lang="ts">
import { useAgentWebSocket } from '~/composables/useAgentWebSocket'

// ✅ 接收WebSocket实时数据
const { agents, isConnected } = useAgentWebSocket()

// Agent ID映射
const AGENT_MAP: Record<string, string> = {
  dispatcher: 'decision_dispatcher',
  analyzer: 'learning_analysis',
  planner: 'path_planning',
  generator: 'knowledge_generation',
  reviewer: 'review_correction',
  judge: 'judge_agent',
  quiz: 'question_generation',
}

// ========== 状态计算函数 ==========

/**
 * 获取Agent状态
 */
const getAgentStatus = (agentId: string): string => {
  const realId = AGENT_MAP[agentId]
  const agent = agents.value.find(a => a.id === realId)
  return agent?.status || 'idle'
}

/**
 * 获取Agent进度（0-100）
 */
const getAgentProgress = (agentId: string): number => {
  const realId = AGENT_MAP[agentId]
  const agent = agents.value.find(a => a.id === realId)
  return agent?.progress || 0
}

/**
 * 获取Agent状态CSS类
 */
const getAgentStatusClass = (agentId: string): string => {
  const status = getAgentStatus(agentId)
  return `status-${status}`
}

/**
 * 获取Agent背景色
 */
const getAgentFill = (agentId: string): string => {
  const status = getAgentStatus(agentId)
  const colorMap: Record<string, string> = {
    idle: '#FFFFFF',         // 白色
    running: '#EEF2FF',      // 淡靛蓝
    completed: '#ECFDF5',    // 淡绿色
    error: '#FEF2F2',        // 淡红色
    paused: '#FFFBEB',       // 淡黄色
  }
  return colorMap[status] || colorMap.idle
}

/**
 * 获取Agent边框色
 */
const getAgentStroke = (agentId: string): string => {
  const status = getAgentStatus(agentId)
  const colorMap: Record<string, string> = {
    idle: '#E5E7EB',         // 灰色
    running: '#4F46E5',      // 靛蓝色
    completed: '#059669',    // 绿色
    error: '#DC2626',        // 红色
    paused: '#D97706',       // 橙色
  }
  return colorMap[status] || colorMap.idle
}

/**
 * 获取Agent状态点颜色
 */
const getAgentDotColor = (agentId: string): string => {
  const status = getAgentStatus(agentId)
  const colorMap: Record<string, string> = {
    idle: '#9CA3AF',         // 灰色
    running: '#4F46E5',      // 靛蓝色
    completed: '#059669',    // 绿色
    error: '#DC2626',        // 红色
    paused: '#D97706',       // 橙色
  }
  return colorMap[status] || colorMap.idle
}

/**
 * 获取Agent描述文本
 */
const getAgentDescription = (agentId: string): string => {
  const status = getAgentStatus(agentId)
  const realId = AGENT_MAP[agentId]
  const agent = agents.value.find(a => a.id === realId)
  
  const defaultDesc: Record<string, string> = {
    dispatcher: '调度中枢 · 任务编排',
    analyzer: '诊断盲区 · 匹配难度',
    planner: '生成学习路径序列',
    generator: 'RAG 生成 · 标注来源',
    reviewer: '质疑 · 提出修正',
    judge: '独立裁决 · 给出依据',
    quiz: '分阶试题 · 动态调整',
  }
  
  if (status === 'running' && agent?.progress) {
    return `执行中... ${agent.progress}%`
  }
  if (status === 'completed') {
    return '已完成 ✓'
  }
  if (status === 'error') {
    return '执行出错 ✗'
  }
  
  return defaultDesc[agentId] || ''
}

// ========== 连接线状态函数 ==========

/**
 * 判断连接线是否激活（源Agent完成，目标Agent运行中）
 */
const isConnectionActive = (fromId: string, toId: string): boolean => {
  const fromStatus = getAgentStatus(fromId)
  const toStatus = getAgentStatus(toId)
  return (fromStatus === 'completed' && toStatus === 'running') ||
         (fromStatus === 'running' && toStatus === 'idle')
}

/**
 * 获取连接线颜色
 */
const getConnectionColor = (fromId: string, toId: string): string => {
  if (isConnectionActive(fromId, toId)) return '#4F46E5'  // 激活：靛蓝色
  if (getAgentStatus(fromId) === 'completed' && getAgentStatus(toId) === 'completed') {
    return '#059669'  // 双方完成：绿色
  }
  return '#9CA3AF'  // 默认：灰色
}

/**
 * 获取连接线箭头标记
 */
const getConnectionMarker = (fromId: string, toId: string): string => {
  if (isConnectionActive(fromId, toId)) return 'url(#arrActive)'
  if (getAgentStatus(fromId) === 'completed' && getAgentStatus(toId) === 'completed') {
    return 'url(#arrCompleted)'
  }
  return 'url(#arr)'
}

/**
 * 判断回环连接线是否激活（裁判驳回，需重新生成）
 */
const isLoopConnectionActive = (fromId: string, toId: string): boolean => {
  const fromStatus = getAgentStatus(fromId)
  const toStatus = getAgentStatus(toId)
  // 裁判完成但知识生成需要重新运行
  return fromStatus === 'completed' && toStatus === 'running'
}

/**
 * 获取回环连接线颜色
 */
const getLoopConnectionColor = (fromId: string, toId: string): string => {
  if (isLoopConnectionActive(fromId, toId)) return '#DC2626'  // 驳回：红色
  return '#4F46E5'  // 默认：靛蓝色
}

/**
 * 获取回环连接线箭头标记
 */
const getLoopConnectionMarker = (fromId: string, toId: string): string => {
  if (isLoopConnectionActive(fromId, toId)) return 'url(#arrActive)'
  return 'url(#arr)'
}
</script>

<style scoped>
.agent-flow-diagram {
  width: 100%;
  height: auto;
  max-width: 920px;
  margin: 0 auto;
  display: block;
}

/* ========== Agent节点动画 ========== */

/* 运行状态：边框脉冲 */
.agent-node.status-running rect {
  animation: pulse-border 2s ease-in-out infinite;
}

/* 完成状态：平滑过渡 */
.agent-node.status-completed rect {
  transition: all 0.3s ease;
}

/* 错误状态：轻微震动 */
.agent-node.status-error rect {
  animation: shake 0.5s ease-in-out;
}

/* ========== 连接线动画 ========== */

/* 流动效果 */
.flowing-line {
  stroke-dasharray: 8 4;
  animation: flow-line 1s linear infinite;
}

/* 进度条动画 */
.progress-bar {
  transition: width 0.3s ease;
}

/* ========== 关键帧定义 ========== */

@keyframes pulse-border {
  0%, 100% {
    stroke-opacity: 1;
    filter: drop-shadow(0 0 0px rgba(79, 70, 229, 0));
  }
  50% {
    stroke-opacity: 0.7;
    filter: drop-shadow(0 0 4px rgba(79, 70, 229, 0.3));
  }
}

@keyframes flow-line {
  0% {
    stroke-dashoffset: 12;
  }
  100% {
    stroke-dashoffset: 0;
  }
}

@keyframes shake {
  0%, 100% {
    transform: translateX(0);
  }
  25% {
    transform: translateX(-2px);
  }
  75% {
    transform: translateX(2px);
  }
}
</style>
```

**效果**：
- ✅ **精美视觉**：完全保留origin的SVG布局和配色
- ✅ **实时状态**：通过WebSocket绑定7个Agent的真实状态
- ✅ **动画效果**：
  - 运行中节点：边框脉冲动画 + 进度条
  - 激活连接线：流动动画（虚线滚动）
  - 完成节点：绿色高亮
  - 错误节点：红色高亮 + 轻微震动
- ✅ **交互能力**：保留触发生成按钮

**数据流**：
```
WebSocket Server
       ↓
useAgentWebSocket.ts
       ↓
agents: Ref<AgentStatus[]>
       ↓
AgentFlowDiagram.vue
       ↓
getAgentStatus('dispatcher') → 'running'
getAgentFill('dispatcher') → '#EEF2FF'
getAgentStroke('dispatcher') → '#4F46E5'
       ↓
SVG rect :fill / :stroke
       ↓
实时渲染
```

---

##### 步骤4.4：组件样式统一（预计1小时）

**策略**：保留agent-system的业务组件，应用origin的CSS类覆盖Nuxt UI默认样式。

**文件**：`agent-system/frontend/assets/css/main.css`（追加）

```css
/* ========== Nuxt UI组件样式覆盖 ========== */

/* UCard → .card样式 */
.u-card,
[class*="bg-white"][class*="rounded"][class*="border"] {
  background: var(--bg) !important;
  border: 1px solid var(--line) !important;
  border-radius: var(--radius) !important;
  box-shadow: none !important;
  padding: 24px !important;
}

/* UButton → .btn样式 */
.u-button,
[class*="inline-flex"][class*="items-center"][class*="rounded"] {
  font-family: var(--font) !important;
  font-size: 13px !important;
  font-weight: 500 !important;
  border-radius: var(--radius-sm) !important;
  transition: all 0.15s !important;
}

/* UBadge → .badge样式 */
.u-badge,
[class*="inline-flex"][class*="items-center"][class*="rounded-full"] {
  border-radius: 20px !important;
  font-size: 11px !important;
  font-weight: 500 !important;
  padding: 3px 8px !important;
}

/* UProgress → .bar样式 */
.u-progress,
[class*="h-"][class*="bg-gray"][class*="rounded-full"] {
  height: 4px !important;
  background: var(--bg-muted) !important;
  border-radius: 99px !important;
  overflow: hidden !important;
}
```

**保留的业务组件**：
- `components/agent-view/AgentFlowChart.vue` - Agent状态列表（保留，用于workflow页面详细展示）
- `components/resource/ResourceTabs.vue` - 资源Tab切换（保留）

**新建的组件**：
- `components/agent-view/AgentFlowDiagram.vue` - 动态SVG流程图（新建，用于首页和workflow页面）

---

#### 第五阶段：样式调优与测试（预计2-3小时）

##### 步骤5.1：响应式适配

**检查点**：
- 760px断点：卡片从2列变为1列
- 900px断点：导航栏折叠为汉堡菜单
- 移动端：表单全宽显示

**操作**：在各页面的scoped样式中添加媒体查询

##### 步骤5.2：Nuxt UI样式覆盖

对于无法替换的Nuxt UI组件，通过CSS覆盖实现样式统一：

```css
/* 覆盖UCard默认样式 */
.u-card {
  border: 1px solid var(--line) !important;
  border-radius: var(--radius) !important;
  box-shadow: none !important;
}

/* 覆盖UButton默认样式 */
.u-button {
  font-family: var(--font) !important;
  border-radius: var(--radius-sm) !important;
}

/* 覆盖UBadge默认样式 */
.u-badge {
  border-radius: 20px !important;
  font-size: 11px !important;
}
```

##### 步骤5.3：视觉一致性检查

**检查清单**：
- [ ] 所有页面使用统一的色彩变量
- [ ] 所有卡片边框圆角一致
- [ ] 所有按钮样式一致
- [ ] 所有进度条样式一致
- [ ] 导航栏毛玻璃效果正常
- [ ] 脉冲动画正常工作
- [ ] 字体渲染清晰（抗锯齿）

##### 步骤5.4：功能回归测试

**测试清单**：
- [ ] 登录/注册功能正常
- [ ] 学习者画像提交正常
- [ ] 学情诊断数据加载正常
- [ ] Agent协同WebSocket连接正常
- [ ] 资源展示Tab切换正常
- [ ] 答题反馈和苏格拉底追问正常
- [ ] 分析报告数据展示正常
- [ ] 历史记录加载正常

---

## 实施优先级与时间规划

### 五、实施优先级

| 阶段 | 任务 | 优先级 | 预计时间 | 依赖 |
|------|------|--------|----------|------|
| 1 | CSS设计系统迁移 | ⭐⭐⭐ 最高 | 2-3小时 | 无 |
| 2 | 布局系统迁移 | ⭐⭐⭐ 高 | 1-2小时 | 阶段1 |
| 3.1 | 首页迁移 | ⭐⭐⭐ 高 | 1小时 | 阶段2 |
| 3.2 | 画像页迁移 | ⭐⭐⭐ 高 | 1小时 | 阶段2 |
| 3.3 | 仪表盘迁移 | ⭐⭐⭐ 高 | 1.5小时 | 阶段2 |
| **3.4** | **登录页重写** | ⭐⭐⭐ 高 | 1小时 | 阶段2 |
| **3.5** | **注册页重写** | ⭐⭐⭐ 高 | 1小时 | 阶段2 |
| 3.6 | Agent协同页迁移 | ⭐⭐ 中 | 1小时 | 阶段2 |
| 3.7 | 资源展示页迁移 | ⭐⭐ 中 | 1小时 | 阶段2 |
| 3.8 | 反馈交互页迁移 | ⭐⭐ 中 | 1小时 | 阶段2 |
| 3.9 | 分析报告页迁移 | ⭐⭐ 中 | 1小时 | 阶段2 |
| 3.10 | 历史记录页调整 | ⭐ 低 | 0.5小时 | 阶段2 |
| **4.1** | **知识雷达图动态化** | ⭐⭐⭐ 高 | 1小时 | 阶段3 |
| **4.2** | **难度匹配曲线动态化** | ⭐⭐⭐ 高 | 1小时 | 阶段3 |
| **4.3** | **Agent流程图动态化** | ⭐⭐⭐⭐⭐ 最高 | 3-4小时 | 阶段3 |
| 4.4 | 组件样式统一 | ⭐⭐ 中 | 1小时 | 4.1-4.3 |
| 5 | 样式调优与测试 | ⭐⭐⭐ 高 | 2-3小时 | 阶段4 |

### 六、总时间估算

- **最短时间**：18小时（连续开发，无阻塞）
- **建议时间**：22-26小时（含测试和调优）
- **保守时间**：30小时（含意外问题处理）

> **重点说明**：
> - 阶段3.4-3.5（登录/注册页重写）是用户入口，需要精美设计
> - 阶段4（核心组件动态化）是本次美化的核心亮点，特别是Agent流程图的动态化改造，预计需要3-4小时。这部分工作将实现"精美视觉+实时功能"的完美结合

---

## 风险与应对

### 七、潜在风险及应对措施

#### 7.1 CSS变量与Tailwind冲突

**风险**：origin的CSS变量可能与Tailwind的默认主题冲突

**应对**：
- 在`tailwind.config.js`中显式映射CSS变量
- 使用`!important`覆盖必要的Tailwind默认值
- 测试所有颜色类名是否正确应用

#### 7.2 Nuxt UI组件样式覆盖困难

**风险**：Nuxt UI组件的内部样式难以覆盖

**应对**：
- 优先使用origin的原生HTML+CSS组件
- 对于必须保留的Nuxt UI组件，使用深度选择器`:deep()`覆盖
- 考虑创建wrapper组件封装Nuxt UI组件

#### 7.3 响应式断点不一致

**风险**：origin使用760px/900px，Tailwind使用sm/md/lg

**应对**：
- 在`tailwind.config.js`中自定义断点：
```javascript
screens: {
  'sm': '640px',
  'md': '760px',  // 对齐origin
  'lg': '900px',  // 对齐origin
  'xl': '1280px',
}
```

#### 7.4 动态数据绑定丢失

**风险**：迁移过程中可能丢失agent-system的数据绑定逻辑

**应对**：
- 迁移前备份agent-system的页面文件
- 逐页面对比，确保所有`v-for`、`v-if`、`@click`等绑定保留
- 迁移后立即测试API调用

#### 7.5 浏览器兼容性

**风险**：`backdrop-filter: blur()`在旧浏览器不支持

**应对**：
- 添加fallback背景色：
```css
.topnav {
  background: rgba(255,255,255,.95); /* fallback */
  backdrop-filter: saturate(180%) blur(12px);
}
```

---

## 验收标准

### 八、验收标准

#### 8.1 视觉验收

- [ ] 导航栏显示毛玻璃效果，与origin一致
- [ ] 所有页面使用统一的CSS变量色彩体系
- [ ] 卡片、按钮、进度条样式与origin一致
- [ ] 首页Hero区域和Agent介绍与origin一致
- [ ] 脉冲动画正常工作
- [ ] **知识雷达图**：靛蓝色配色、浅灰网格线、无背景区域
- [ ] **难度匹配曲线**：纯折线图、靛蓝渐变背景、灰色虚线参考线
- [ ] **Agent流程图**：SVG节点精美、连接线流畅、动画效果正常

#### 8.2 登录/注册页验收 ⭐ 新增

- [ ] 页面顶部**无导航栏**
- [ ] 页面垂直水平居中，最大宽度420px
- [ ] 背景有浮动的装饰圆形元素
- [ ] 顶部显示品牌标识（靛蓝色圆点+系统名称）
- [ ] 表单卡片有边框和圆角，与origin风格一致
- [ ] 输入框有图标（用户名、邮箱、密码）
- [ ] 输入框聚焦时显示靛蓝色边框和阴影
- [ ] 注册页面有密码强度指示器
- [ ] 按钮使用靛蓝色背景，hover时变深
- [ ] 底部有版权信息
- [ ] 响应式布局在移动端正常

#### 8.3 功能验收

- [ ] 登录/注册/退出功能正常
- [ ] 学习者画像提交成功，API返回正确
- [ ] 学情诊断仪表盘数据动态加载
- [ ] Agent协同页WebSocket实时连接
- [ ] 资源展示Tab切换和Markdown渲染正常
- [ ] 答题反馈和苏格拉底追问正常工作
- [ ] 分析报告数据正确展示
- [ ] 历史记录时间线正常显示

#### 8.3 动态组件验收 ⭐ 新增

**知识雷达图**：
- [ ] 数据从API动态获取，非硬编码
- [ ] hover显示具体分数
- [ ] 图表尺寸适配容器

**难度匹配曲线**：
- [ ] 数据从API动态获取，非硬编码
- [ ] 学习者水平线正确显示
- [ ] hover显示资源名称和难度值

**Agent流程图**：
- [ ] WebSocket连接状态正确显示
- [ ] 7个Agent节点状态实时更新
- [ ] 运行中节点显示脉冲动画
- [ ] 运行中节点显示进度条
- [ ] 激活连接线显示流动动画
- [ ] 完成节点显示绿色高亮
- [ ] 错误节点显示红色高亮
- [ ] 触发生成按钮功能正常
- [ ] 回环连线（需修正）正确显示

#### 8.3 性能验收

- [ ] 首屏加载时间 < 3秒
- [ ] 页面切换无明显卡顿
- [ ] WebSocket连接稳定，无断连

#### 8.4 响应式验收

- [ ] 桌面端（1280px+）布局正常
- [ ] 平板端（760px-1280px）布局正常
- [ ] 移动端（<760px）布局正常，汉堡菜单工作

---

## 附录

### 附录A：关键文件对照表

| 文件类型 | origin路径 | agent-system路径 | 操作 |
|----------|-----------|------------------|------|
| CSS设计系统 | `assets/css/main.css` | `assets/css/main.css` | 替换 |
| 默认布局 | `layouts/default.vue` | `layouts/default.vue` | 整合 |
| 首页 | `pages/index.vue` | `pages/index.vue` | 替换 |
| 画像页 | `pages/profile.vue` | `pages/profile.vue` | 整合 |
| 仪表盘 | `pages/dashboard.vue` | `pages/dashboard.vue` | 整合 |
| Agent协同 | `pages/workflow.vue` | `pages/workflow.vue` | 整合 |
| 资源展示 | `pages/resources.vue` | `pages/resources.vue` | 整合 |
| 反馈交互 | `pages/practice.vue` | `pages/practice.vue` | 整合 |
| 分析报告 | `pages/report.vue` | `pages/report.vue` | 整合 |
| SVG流程图 | `components/AgentFlowDiagram.vue` | `components/AgentFlowDiagram.vue` | 复制 |

### 附录B：CSS变量速查表

```css
/* 中性色阶 */
--bg: #FFFFFF;           /* 背景色 */
--bg-soft: #FAFAFA;      /* 柔和背景 */
--bg-muted: #F5F5F5;     /* 静默背景 */
--text: #111827;         /* 主文本 */
--text-2: #4B5563;       /* 次要文本 */
--text-3: #9CA3AF;       /* 辅助文本 */
--line: #ECECEF;         /* 边框线 */
--line-2: #E5E7EB;       /* 次要边框 */

/* 强调色 */
--accent: #4F46E5;       /* 主强调色（靛蓝） */
--accent-soft: #EEF2FF;  /* 柔和强调色 */

/* 语义色 */
--ok: #059669;           /* 成功/完成 */
--ok-soft: #ECFDF5;
--warn: #D97706;         /* 警告/进行中 */
--warn-soft: #FFFBEB;
--err: #DC2626;          /* 错误/失败 */
--err-soft: #FEF2F2;
```

### 附录C：实施检查清单

**阶段1完成检查**：
- [ ] `main.css`包含完整的CSS变量定义
- [ ] `tailwind.config.js`正确映射CSS变量
- [ ] 开发服务器启动无CSS错误

**阶段2完成检查**：
- [ ] 导航栏显示毛玻璃效果
- [ ] 登录/注册按钮正常工作
- [ ] 移动端汉堡菜单正常
- [ ] 底部版权信息显示

**阶段3完成检查**：
- [ ] 首页Hero区域正常显示
- [ ] 画像页表单提交成功
- [ ] 仪表盘数据动态加载
- [ ] Agent协同页WebSocket连接
- [ ] 资源展示Tab切换正常
- [ ] 答题反馈功能正常
- [ ] 分析报告数据正确

**阶段4完成检查**：
- [ ] SVG流程图在首页正常渲染
- [ ] 所有业务组件样式统一

**阶段5完成检查**：
- [ ] 响应式布局在各断点正常
- [ ] 视觉一致性检查通过
- [ ] 功能回归测试通过

---

**文档版本**：v3.0
**创建日期**：2026-06-27
**最后更新**：2026-06-27

---

## 更新日志

### v3.0 (2026-06-27)
- ✅ 新增**登录/注册页面重写方案**
- ✅ 登录页面完整实现代码（禁用导航栏、origin设计风格）
- ✅ 注册页面完整实现代码（禁用导航栏、密码强度指示器）
- ✅ 更新实施步骤（新增3.4-3.5步骤）
- ✅ 更新时间规划（增加2小时）
- ✅ 新增登录/注册页验收标准

### v2.0 (2026-06-27)
- ✅ 新增**核心组件动态化改造方案**（第四阶段）
- ✅ 新增**知识雷达图**完整实现代码（精美配置+动态数据）
- ✅ 新增**难度匹配曲线**完整实现代码（精美配置+动态数据）
- ✅ 新增**Agent流程图**完整实现代码（动态SVG+WebSocket+CSS动画）
- ✅ 更新实施优先级和时间规划（增加5-7小时）
- ✅ 新增动态组件验收标准
- ✅ 新增Nuxt UI组件样式覆盖方案

### v1.0 (2026-06-27)
- 初始版本
- CSS设计系统迁移方案
- 布局系统迁移方案
- 页面迁移方案
- 基础验收标准

# 管理后台静态托管：SPA history 路由回退。
# 资源以 /admin/ 为 base 构建，统一网关会把 /admin/ 前缀代理到本容器根路径。
server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }
}
